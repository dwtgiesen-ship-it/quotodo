{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "export DIRECT_URL=\"${DIRECT_URL:-${DATABASE_URL_UNPOOLED:-$DATABASE_URL}}\" && prisma generate && prisma db push && next build",
  "framework": "nextjs"
}
